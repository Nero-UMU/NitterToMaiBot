# 更新日志

## 1.5.7

### 细节

- 推文翻译超时、调用失败或返回空内容时继续发送原文，并在翻译区域显示“翻译错误”，不再阻断自动推送、手动获取和链接解析。

## 1.5.6

### 细节

- 插件 ID 按插件中心推荐格式调整为 `github.nero-umu.nitter-to-maibot`，并自动迁移旧 ID 数据目录中的订阅和去重状态。
- Manifest 补齐主页、文档和问题反馈地址，插件类型调整为外部集成，并移除代码未使用的 `config.get` 能力声明。

## 1.5.5

### 细节

- 项目许可证由 MIT 调整为 `GPL-3.0-or-later`，并同步更新完整许可证文本与插件元信息。

## 1.5.4

### 细节

- 按插件发布规范停止跟踪运行时 `config.toml`，由 MaiBot Runner 根据配置模型生成并在本地保留。
- 补充插件能力、故障排查和依赖安装说明，并修正项目与作者链接。

## 1.5.3

### 主要功能

- 新增可选的静默时段，默认关闭且默认范围为北京时间 00:00–06:00；期间暂停自动订阅推送，结束后的下一轮扫描统一发送积压推文。

### 细节

- 静默期间发现的推文会持久化到插件状态文件，配置热更新、插件重载或 MaiBot 重启后仍可继续发送。
- 静默积压超过 1 条时强制使用一条 QQ 合并转发，不受普通合并转发阈值影响；手动获取和链接解析不受静默时段影响。

## 1.5.2

### 主要功能

- 新增 `/推特帮助` 命令及 `/twitter_help` 英文别名，用于查看全部命令、参数和简要说明。

## 1.5.1

### 细节

- 后台新增可编辑的“翻译提示词”，默认预填原有翻译要求，保存后用于后续所有推文翻译。

## 1.5.0

### 主要功能

- 新增可选的推文正文翻译；自动推送、`/推特推文`、`/推特解析` 和自动链接解析统一支持在原文下附加简体中文翻译。

### 细节

- 后台新增“启用推文翻译”和“翻译模型”，可选择 MaiBot 已配置的 `utils`、`replyer` 或 `planner` 模型任务，默认关闭并推荐使用 `utils`。
- 翻译通过 `maibot-plugin-sdk` 的 `llm.generate` 能力执行；失败时保留推文待后续重试，不会把未翻译内容标记为成功投递。

## 1.4.5

### 细节

- “轮询间隔（秒）”默认值由 300 秒调整为 600 秒，并同步更新后台提示。

## 1.4.4

### 主要功能

- `/推特订阅` 列表增加推特显示名；超过 20 个账号时自动按每 20 个一页打包为 QQ 合并转发。

### 细节

- 从 Nitter RSS 频道标题提取并持久化账号显示名，后台只读订阅镜像同步展示该字段。
- 订阅列表统一使用“序号.@ID 推特名”格式，名称暂不可用时明确显示“名称未获取”。

## 1.4.3

### 细节

- 后台“包含转推”改名为“转发转推”，并明确说明该选项对自动订阅扫描、`/推特推文` 和 `/推特解析` 的影响。

## 1.4.2

### 细节

- Nitter 实例地址默认值和后台占位提示改为 `https://nitter.net`。
- 推文原文链接统一使用 `x.com` 官方域名，不再生成旧版 Twitter 域名链接。

## 1.4.1

### 细节

- 自动推文链接解析改为阻塞型 Hook；插件识别并处理链接后中止后续消息链，避免 LLM 重复回复。
- 媒体大小上限默认值和最大值恢复为 10 MiB。

## 1.4.0

### 主要功能

- 自动推文链接解析扩展到 QQ 私聊和群聊，默认开启，后台名称调整为“自动解析推文链接”。
- 每群订阅账号上限默认改为 `0`，并将 `0` 定义为不限制订阅数量。
- 媒体大小上限默认提升至 100 MiB，同时保留 10 MiB 的 SDK 内嵌图片安全上限。

### 细节

- “发送机器人 QQ 号”的后台示例改为通用脱敏号码 `123456`，不再展示实际机器人号码。

## 1.3.3

### 细节

- 推文正文改为简洁展示：`@账号 · 时间（北京时间）`，正文下方仅保留原文链接。
- 转推使用 `@订阅账号 转推了 @作者` 的标题格式。
- 时间固定按 `Asia/Shanghai` 转换为北京时间，不再依赖服务器本地时区。

## 1.3.2

### 细节

- 移除推文消息顶部的 `【Nitter 推文更新】` 标题，消息直接从订阅账号信息开始。

## 1.3.1

### 细节

- 后台配置项全部使用简体中文标签，并为每个可见设置补充用途、影响和建议值说明。
- 明确说明“发送机器人 QQ 号”不是目标群号，并将配置版本改为只读字段。
- 为订阅群列表和账号订阅列表补充只读数据结构说明。

## 1.3.0

### 主要功能

- 将 QQ 群和推特账号订阅合并为 `subscriptions.json` 中的统一多对多数据结构，扫描与投递只读取这一真实数据源。
- 后台新增“订阅列表（只读）”，展示订阅群、群推送开关，以及每个账号对应的订阅群列表；群内命令修改后自动同步展示。

### 细节

- 自动迁移 1.2.x 的按群订阅文件，并把旧配置中的全局账号与目标群订阅合并到统一存储。
- 迁移完成后清空并隐藏旧 `nitter.accounts`、`delivery.qq_groups` 字段，避免出现两个可修改来源。
- 当前群取关最后一个账号时同步移除空群记录；未订阅的群不能通过推送开关命令创建空记录。
- 后台保存其他配置时会忽略并修复只读订阅镜像，订阅关系仍以 `subscriptions.json` 为准。

## 1.2.0

### 主要功能

- 新增 `forward_batch_threshold` 配置；同一轮向同一聊天流发送的推文数超过阈值时，使用 QQ 合并转发打包，默认超过 1 条即打包。
- `/推特推文` 和自动轮询均支持按阈值打包；自动轮询会合并同一目标群内来自多个订阅账号的本轮更新。

### 细节

- 合并转发中的每条推文使用独立聊天记录节点，并保留正文、Twitter 原文链接、图片及媒体文件信息。
- 为插件声明 `send.forward` SDK 能力，并保持单条推文的原有发送方式不变。

## 1.1.3

### 细节

- 为 `/推特推文 <账号> [数量]` 增加英文别名 `/twitter_posts`。

## 1.1.2

### 细节

- 修复手动获取多条推文时，单个媒体附件发送失败会中止剩余推文的问题。
- “查看原文”改为 Twitter 官方状态链接，不再暴露 Nitter 实例地址。

## 1.1.1

### 主要功能

- 将最新推文测试命令替换为 `/推特推文 <账号> [数量]`，支持一次获取指定数量的最新推文，默认获取 1 条。

### 细节

- 移除 `/推特测试` 和 `/twitter_test` 命令。
- 手动获取数量受 `max_posts_per_scan` 配置限制，且不会改变自动轮询游标。

## 1.1.0

### 主要功能

- 新增按 QQ 群独立持久化的动态订阅，并与配置文件全局订阅合并轮询。
- 新增群内关注、取关、订阅列表、推送开关和最新推文测试命令。
- 新增推文链接手动解析，以及基于观察型消息 Hook 的可选自动识别。
- 新增转推识别和 `include_retweets` 过滤配置。

### 细节

- 修复 RSS 推文链接中的 `#m` 页面锚点被带入 HTTP 请求、导致部分 Nitter 实例返回 404 的问题。
- Nitter 请求采用浏览器兼容请求头，提高自建实例与反向代理兼容性。
- 状态页解析限定在主推文容器，排除评论和引用推文媒体。
- 使用 Twitter Snowflake ID 还原手动解析推文的发布时间。
- 视频和其他文件改用 URL 文件负载，图片逐张发送，规避 SDK 进程间通信的 16 MiB 单帧限制。
- 新增动态订阅存储、命令链和状态页解析单元测试。

## 1.0.0

### 主要功能

- 支持按推特 @ID 订阅 Nitter RSS，并定时扫描新推文。
- 支持把推文正文、图片、视频及其他附件转发到多个 QQ 群。
- 支持首次运行去重基线、持久化投递进度和失败后的精确续传。

### 细节

- 提供 `/nitter_status` 和 `/nitter_scan` 联调命令。
- 支持插件配置热更新和多 QQ 机器人账号路由。
- 使用 `maibot-plugin-sdk` 能力接口，不依赖 MaiBot 主程序内部模块。
